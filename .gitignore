node_modules/
dist/
coverage/
*.log

# local stores created during dev/testing
.braincontext/
*.db
*.db-shm
*.db-wal
*.sqlite

.env
.env.local

# local test credentials (Turso creds live in .env.local / env only — never commit)
test-config.md