---
name: demo-skill
description: A tiny demo skill used to exercise `bctx skill add/export` round-tripping.
allowed-tools: Bash(echo:*)
metadata:
  version: "1.0"
---

# Demo skill

A minimal skill bundle: a script under `scripts/` and a reference under `references/`.

## Usage

```bash
bash scripts/run.sh
```
